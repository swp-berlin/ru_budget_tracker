[project]
name = "importer"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Tom Rüger", email = "tom.rueger@swp-berlin.org" }
]
requires-python = ">=3.13"
dependencies = [
    "alembic>=1.18.4",
    "httpx2>=2.4.0",
    "pydantic>=2.13.0",
    "pydantic-settings>=2.13.1",
    "python-dotenv>=1.2.2",
]

[build-system]
requires = ["uv_build>=0.10.2,<0.11.0"]
build-backend = "uv_build"
